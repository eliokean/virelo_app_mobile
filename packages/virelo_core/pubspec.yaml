name: virelo_core
description: Core package for Virelo containing shared logic.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  local_auth: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
