name: virelo_core
description: Core package for Virelo containing shared logic.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  local_auth: ^2.1.8
  cryptography: ^2.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.5.3
  ed25519_edwards: ^0.3.1
  nfc_manager: ^3.4.0
  device_info_plus: ^13.2.0
  freerasp: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.6.4
  build_runner: ^2.15.0
